<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hello Food</title>
  <link rel="stylesheet" href="/stylesheets/Master.css">
</head>
<body>
  <div class="container">
    <nav>
      <ul>
        <li id="nav-brand">Hello Food</li>
        <li><a href="/">Food List</a></li>
        <li><a href="/categories">Category List</a></li>
        <li id="nav-user">User &#9207;</li>
      </ul>
    </nav>

    <div class="content">
      <h1>Edit Food</h1>
      <form action="/items/update/<%= item.id %>" method="POST">
        <table>
          <tr>
            <td class="w-25"><label for="food-name" class="form-label">Food Name</label></td>
            <td>: <input type="text" id="food-name" class="text-field" name="food-name" value="<%= item.name %>" required></td>
          </tr>
          <tr>
            <td class="w-25"><label for="food-price" class="form-label">Food Price</label></td>
            <td>: <input type="number" id="food-price" class="text-field" name="food-price" value="<%= item.price %>" required></td>
          </tr>
          <tr>
            <td class="form-text-area w-25" class="form-label"><label for="food-desc">Food Description</label></td>
            <td>: <textarea class="form-text-area" rows="3" id="food-desc" name="food-desc"><%= item.description %></textarea></td>
          </tr>
          <tr>
            <td class="w-25"><label>Food Categories</label></td>
            <td>:
              <% categories.each do |category| %>
                <input type="checkbox" name="food-categories[]" id="<%= category.id %>"
                       value="<%= category.id %>" <%= item.category_included?(category.id) ? "checked" : "" %>>
                  <%= category.name %>
                </input>
              <% end %>
            </td>
          </tr>
        </table>
        <br><br>
        <button type="submit" class="btn-primary float-left">Update Food</button>
      </form>
      <a href="/items"><button class="btn-secondary ml-4">Back</button></a>
    </div>

    <footer>Clapie &#169; 2020</footer>
  </div>
</body>
</html>
